{
  "name": "movies",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install:sqlite": "npm i sqlite3 -D && rm -rf node_modules && npm i && npm rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cli-select": "^1.1.2",
    "dotenv": "^8.2.0",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "sqlite3": "^5.0.2"
  }
}
